name: Update RSS Feed

on:
  schedule:
    # Runs at 10:20 AM UTC daily
    - cron: '20 10 * * *'

jobs:
  update_rss:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Update RSS
      run: |
        echo "Your script to update the RSS feed goes here"
        # For example, a Python script that generates the RSS feed
        # python update_rss.py

    - name: Commit and Push
      run: |
        git config --global user.email "liamcolless@github.com"
        git config --global user.name "liamcolless"
        git add 032c_rss.xml
        git commit -m "Update RSS feed" || echo "No changes to commit"
        git push
